name: CI

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: swift-institute/.github/.github/workflows/swift-ci.yml@coenttb/revert-303-ci-startup-failure
    secrets: inherit
