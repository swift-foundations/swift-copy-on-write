name: CI

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: swift-institute/.github/.github/workflows/swift-ci.yml@coenttb/test-revert-309
    secrets: inherit
