name: CI

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: swift-institute/.github/.github/workflows/swift-ci.yml@65a2b61037f4b61989319a02fc2548158c7ee916
    secrets: inherit
